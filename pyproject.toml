[project]
name = "openops"
version = "0.1.0"
description = "AI-powered multi-cloud infrastructure operations platform"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
    "detect-secrets>=1.5.0",
]
