accounts:
  # AWS accounts (existing)
  extra:
    - id: "111111111111"
      name: "partner-staging"
      role_name: "OpsAgentPartnerRole"
      regions: ["us-east-1"]

  overrides:
    "034362076319":
      alias: "hub-account"
    "999999999999":
      skip: true

  defaults:
    role_name: "OpsAgentReadOnly"
    regions: ["us-east-1"]

  # Multi-cloud accounts
  clouds:
    # alicloud:
    #   - name: "production"
    #     region: "cn-hangzhou"
    #     access_key_env: "ALICLOUD_PROD_AK"
    #     secret_key_env: "ALICLOUD_PROD_SK"
    # azure:
    #   - name: "prod-subscription"
    #     subscription_id: "xxxx-xxxx"
    #     tenant_id: "xxxx-xxxx"
    #     client_id_env: "AZURE_CLIENT_ID"
    #     client_secret_env: "AZURE_CLIENT_SECRET"
    # gcp:
    #   - name: "my-project"
    #     project_id: "my-project-id"
    #     credentials_env: "GCP_CREDENTIALS_JSON"
