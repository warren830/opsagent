controller:
  replicas: 1
  metrics:
    enabled: true
    serviceMonitor:
      enabled: false          # Use Alloy scrape, not Prometheus Operator
  nodeSelector:
    karpenter.sh/nodepool: common-nodepool
dashboard:
  enabled: false              # Ops provides its own dashboard
installCRDs: true
