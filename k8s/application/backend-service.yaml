apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: openops
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - port: 3080
    targetPort: 3080
    protocol: TCP
    name: http
  selector:
    app: backend
